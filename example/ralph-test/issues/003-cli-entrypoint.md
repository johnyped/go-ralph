# 003 — CLI entrypoint

Add a `main.go` that prints the greeting.

## Acceptance Criteria

- [ ] `main.go` exists at the project root with `package main`
- [ ] Running `go run main.go World` prints `Hello, World!`
- [ ] `go build .` succeeds with no errors
- [ ] `bash test.sh` exits 0 with all checks passed
