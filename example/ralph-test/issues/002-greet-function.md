# 002 — Add Greet function

Implement `Greet` in the `hello` package using TDD.

## Acceptance Criteria

- [ ] `hello/hello.go` exports `func Greet(name string) string` returning `"Hello, <name>!"`
- [ ] `hello/hello_test.go` exists with at least one test covering `Greet`
- [ ] `go test ./hello/...` passes
