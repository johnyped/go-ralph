# 001 — Scaffold Go module

Initialise a Go module for the `ralph-test` project.

## Acceptance Criteria

- [ ] `go.mod` exists with module path `ralph-test` and `go 1.21`
- [ ] `hello/` directory exists
- [ ] `hello/hello.go` exists with `package hello`
